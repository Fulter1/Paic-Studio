import {CONFIG} from './config.js';
import {store} from './store.js';
import {TEMPLATES,SNIPPETS,templateCards} from './templates.js';
import {blank,defaults,validate,plainText,paper} from './document.js';
import {connect,cloudUpsert,cloudList,cloudDelete} from './cloud.js';

const app=document.querySelector('#app');
const toast=document.querySelector('#toast');
const state={page:'home',step:1,query:'',theme:store.theme(),editor:defaults(store.draft()||{}),cloud:'جاري الاتصال',zoom:CONFIG.zoom.default,snippetTab:'opening'};
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const notify=(msg,type='ok')=>{toast.textContent=msg;toast.className=`toast show ${type}`;clearTimeout(notify.t);notify.t=setTimeout(()=>toast.className='toast',2600)};
const records=()=>store.records().sort((a,b)=>new Date(b.updatedAt)-new Date(a.updatedAt));
const nextNumber=()=>{const nums=records().map(r=>Number(String(r.number||'').match(/(\d+)$/)?.[1]||0));return `PAIC-${String((Math.max(0,...nums)+1)).padStart(4,'0')}`};
function applyTheme(){const t=state.theme==='system'?(matchMedia('(prefers-color-scheme:light)').matches?'light':'dark'):state.theme;document.documentElement.dataset.theme=t}
function shell(content){app.innerHTML=`<aside class="sidebar"><div class="brand"><span class="brand-mark"><img src="assets/logo-club.svg"></span><div><b>PAIC</b><small>STUDIO</small></div></div><div class="workspace"><span>WORKSPACE</span><strong>استديو الخطابات</strong></div><nav>${nav('home','⌂','الرئيسية')} ${nav('create','＋','إنشاء خطاب')} ${nav('library','▤','المحفوظات')} ${nav('templates','▦','القوالب')}</nav><div class="side-section"><span>TOOLS</span><button data-page="snippets">✦ <b>النصوص الجاهزة</b></button><button data-page="settings">⚙ <b>الإعدادات</b></button></div><div class="sidebar-footer"><span class="status-dot"></span><div><b>PAIC Cloud</b><small>${esc(state.cloud)}</small></div></div></aside><main class="main"><header class="topbar"><div><span class="eyebrow">PAIC STUDIO / ${label()}</span><h1>${title()}</h1></div><div class="top-actions"><span class="cloud-status"><i></i>${esc(state.cloud)}</span><button class="icon-btn" data-action="theme" title="تغيير المظهر">◐</button><a class="primary top-create" href="#/create">إنشاء خطاب <span>＋</span></a></div></header><div class="content">${content}</div></main><div class="mobile-bar"><button data-action="menu">☰</button><b>PAIC</b><a href="#/create">＋</a></div>`;applyTheme()}
function nav(p,icon,text){return `<a class="nav-link ${state.page===p?'active':''}" href="#/${p}"><i>${icon}</i><span>${text}</span></a>`}
function label(){return ({home:'OVERVIEW',create:'EDITOR',library:'LIBRARY',templates:'TEMPLATES',snippets:'SNIPPETS',settings:'SETTINGS'})[state.page]||'PAIC STUDIO'}
function title(){return ({home:'لوحة التحكم',create:'إنشاء خطاب',library:'مكتبة الخطابات',templates:'القوالب',snippets:'النصوص الجاهزة',settings:'الإعدادات'})[state.page]||'PAIC Studio'}
function home(){const rs=records();const month=rs.filter(r=>r.updatedAt?.slice(0,7)===new Date().toISOString().slice(0,7)).length;return `<section class="hero"><div class="hero-copy"><span class="hero-chip"><i></i> PAIC DOCUMENT STUDIO</span><h2>أنشئ خطابك الرسمي<br><em>بشكل يليق باسم النادي.</em></h2><p>مساحة عمل متكاملة لإنشاء الخطابات الرسمية واختيار القالب وإدارة النصوص والمحفوظات من مكان واحد.</p><div class="hero-actions"><a class="primary big" href="#/create">ابدأ خطابًا جديدًا <span>←</span></a><a class="ghost big" href="#/templates">استعراض القوالب</a></div><div class="hero-trust"><span>✓ حفظ تلقائي</span><span>✓ معاينة مباشرة</span><span>✓ جاهز للطباعة</span></div></div><div class="hero-visual"><div class="orb orb-a"></div><div class="orb orb-b"></div><div class="float-card card-a"><small>LETTER NUMBER</small><b>${esc(rs[0]?.number||'PAIC-0001')}</b><span>آخر خطاب محفوظ</span></div><div class="float-card card-b"><small>WORKFLOW</small><b>01 — 02 — 03</b><span>قالب · بيانات · محتوى</span></div><div class="hero-paper">${paper(defaults({number:'PAIC-0001',eventName:'خطاب رسمي',recipient:'الجهة المستهدفة',message:'مساحة معاينة بسيطة توضح شكل الخطاب قبل الحفظ.',prName:'العلاقات العامة'}))}</div></div></section><section class="stats"><div><small>إجمالي الخطابات</small><strong>${rs.length}</strong><span>محفوظ محليًا</span></div><div><small>هذا الشهر</small><strong>${month}</strong><span>خطابات محدثة</span></div><div><small>القوالب</small><strong>04</strong><span>هويات جاهزة</span></div><div><small>النصوص</small><strong>${Object.values(SNIPPETS).flat().length+Object.values(store.snippets()).flat().length}</strong><span>خيارات جاهزة</span></div></section><section class="home-grid"><div class="panel recent-panel"><div class="section-head"><div><span>RECENT LETTERS</span><h3>آخر الخطابات</h3></div><a href="#/library">عرض الكل ←</a></div>${rs.slice(0,5).map(recordRow).join('')||empty('لا توجد خطابات بعد','ابدأ أول خطاب لك من المحرر.')}</div><div class="panel quick-panel"><span>QUICK START</span><h3>ابدأ من نقطة جاهزة</h3><div class="quick-list"><a href="#/create?template=classic"><b>01</b><span><strong>خطاب رسمي</strong><small>كلاسيكي أكاديمي</small></span><i>←</i></a><a href="#/create?template=partnership"><b>02</b><span><strong>شراكة ورعاية</strong><small>شراكة وتعاون</small></span><i>←</i></a><a href="#/create?template=thanks"><b>03</b><span><strong>شكر وتقدير</strong><small>شكر وتكريم</small></span><i>←</i></a></div></div></section>`}
function recordRow(r){const t=TEMPLATES[r.template]||TEMPLATES.classic;return `<div class="record-row"><span class="record-icon" style="--accent:${t.accent}">${esc((r.number||'PAIC').slice(-2))}</span><div><strong>${esc(r.eventName)}</strong><small>${esc(r.recipient)} · ${esc(r.number)}</small></div><span class="record-type">${t.tag}</span><button data-open="${r.id}">فتح</button></div>`}
function empty(a,b){return `<div class="empty"><span>PAIC</span><strong>${a}</strong><small>${b}</small><a class="primary" href="#/create">إنشاء خطاب</a></div>`}
function create(){const e=state.editor;return `<section class="editor-page"><div class="editor-toolbar"><div><span class="eyebrow">DOCUMENT EDITOR</span><h2>${esc(e.number||'خطاب جديد')}</h2></div><div class="editor-actions"><span class="save-state"><i></i><b id="saveState">محفوظ محليًا</b></span><button class="ghost" data-action="reset">مسودة جديدة</button><button class="primary" data-action="save">حفظ الخطاب</button></div></div><div class="editor-shell"><section class="editor-panel panel"><div class="step-nav">${step(1,'الهوية','القالب والمقاس')} ${step(2,'البيانات','الجهة والتفاصيل')} ${step(3,'المحتوى','النص والتواصل')}</div><div class="editor-content">${state.step===1?stepIdentity():state.step===2?stepData():stepContent()}</div><div class="editor-footer"><button class="ghost" data-action="prev" ${state.step===1?'disabled':''}>السابق</button><div><span>${state.step} / 3</span><button class="primary" data-action="next">${state.step===3?'حفظ الخطاب':'متابعة ←'}</button></div></div></section><aside class="preview-panel panel"><div class="preview-toolbar"><div><span>LIVE PREVIEW</span><strong>${TEMPLATES[e.template].name} · ${e.size==='a4'?'A4':'1:1'}</strong></div><div class="preview-tools"><button data-action="fit">ملاءمة</button><button data-action="zoomOut">−</button><b id="zoomValue">${Math.round(state.zoom*100)}%</b><button data-action="zoomIn">＋</button></div></div><div class="preview-stage"><div id="paperFrame" style="transform:scale(${state.zoom})">${paper(e)}</div></div><div class="preview-footer"><button class="ghost" data-action="copy">نسخ النص</button><button class="ghost" data-action="print">طباعة / PDF</button></div></aside></div></section>`}
function step(n,name,sub){return `<button class="step ${state.step===n?'active':''}" data-step="${n}"><b>0${n}</b><span><strong>${name}</strong><small>${sub}</small></span></button>`}
function stepIdentity(){return `<div class="section-title"><span>01</span><div><h3>هوية الخطاب</h3><p>اختر التصميم أولًا ثم حدد مقاس الورقة.</p></div></div><div class="template-grid">${templateCards(state.editor.template)}</div><div class="option-card"><div><b>مقاس الورقة</b><small>A4 للمخاطبات الرسمية · 1:1 للتصاميم المختصرة</small></div><div class="segmented">${['a4','square'].map(x=>`<button class="${state.editor.size===x?'active':''}" data-size="${x}">${x==='a4'?'A4':'1 : 1'}</button>`).join('')}</div></div>`}
function field(id,label,type='text',wide=false,placeholder=''){const v=state.editor[id]??'';return `<label class="field ${wide?'wide':''}"><span>${label}</span>${type==='textarea'?`<textarea data-field="${id}" placeholder="${placeholder}">${esc(v)}</textarea>`:`<input data-field="${id}" type="${type}" value="${esc(v)}" placeholder="${placeholder}" maxlength="1000">`}</label>`}
function stepData(){return `<div class="section-title"><span>02</span><div><h3>بيانات الخطاب</h3><p>المعلومات الأساسية التي ستظهر في المستند النهائي.</p></div></div><div class="form-grid">${field('recipient','الجهة المستهدفة *','text',false,'مثال: شركة ...')}${field('recipientName','اسم المسؤول','text',false,'اختياري')}${field('date','التاريخ *','date')}${field('eventName','الفعالية / المبادرة *','text',true,'عنوان الخطاب أو الفعالية')}${field('extraParam','نوع الرعاية / التعاون','text',true,'مثال: رعاية ذهبية')}${field('value','القيمة / المبلغ','text',false,'اختياري')}</div><div class="hint"><b>نصيحة</b><span>استخدم اسم الجهة كما تريد ظهوره في الخطاب الرسمي لتفادي التعديل بعد الطباعة.</span></div>`}
function stepContent(){return `<div class="section-title"><span>03</span><div><h3>المحتوى والتواصل</h3><p>استخدم نصًا جاهزًا أو اكتب محتواك بالكامل.</p></div></div><div class="content-tools"><button class="tool-tab active">النص</button><a href="#/snippets">مكتبة النصوص ↗</a></div>${field('message','نص الخطاب *','textarea',true,'اكتب محتوى الخطاب هنا...')}<div class="snippet-strip"><div><b>إدراج سريع</b><small>اختر نصًا جاهزًا وسيضاف للمحرر</small></div><div class="snippet-buttons">${SNIPPETS.opening.slice(0,3).map((x,i)=>`<button data-snippet="${i}">${esc(x[0])}</button>`).join('')}</div></div><div class="form-grid compact">${field('prName','مسؤول العلاقات العامة')}${field('prContact','رقم التواصل')}${field('prEmail','البريد الإلكتروني','email',true)}</div><label class="toggle"><input data-field="digitalStamp" type="checkbox" ${state.editor.digitalStamp?'checked':''}><span></span><b>إظهار الختم الرقمي في الخطاب</b></label>`}
function library(){let rs=records();if(state.query.trim()){const q=state.query.toLowerCase();rs=rs.filter(r=>`${r.number} ${r.recipient} ${r.eventName} ${r.message}`.toLowerCase().includes(q))}return `<section class="library-page"><div class="page-intro"><div><span class="eyebrow">DOCUMENT LIBRARY</span><h2>مكتبة الخطابات</h2><p>كل خطاباتك في مساحة مرتبة وسهلة الوصول.</p></div><a class="primary" href="#/create">＋ إنشاء خطاب</a></div><div class="library-controls"><div class="search-box"><span>⌕</span><input id="search" value="${esc(state.query)}" placeholder="ابحث بالرقم أو الجهة أو الفعالية"></div><span>${rs.length} نتيجة</span></div><div class="library-table"><div class="table-head"><span>الخطاب</span><span>الجهة</span><span>القالب</span><span>التحديث</span><span></span></div>${rs.map(r=>{const t=TEMPLATES[r.template]||TEMPLATES.classic;return `<div class="table-row"><div><b>${esc(r.number)}</b><small>${esc(r.eventName)}</small></div><span>${esc(r.recipient)}</span><em style="--accent:${t.accent}">${t.name}</em><span>${new Date(r.updatedAt).toLocaleDateString('ar-SA')}</span><div class="row-actions"><button data-open="${r.id}">فتح</button><button class="danger" data-delete="${r.id}">حذف</button></div></div>`}).join('')||empty('المكتبة فارغة','ابدأ بإنشاء خطاب جديد.')}</div></section>`}
function templatesPage(){return `<section class="templates-page"><div class="page-intro"><div><span class="eyebrow">TEMPLATE SYSTEM</span><h2>استديو القوالب</h2><p>قوالب مصممة كأنها هويات مستقلة وليست مجرد ألوان مختلفة.</p></div><a class="ghost" href="#/create">فتح المحرر</a></div><div class="template-showcase">${Object.entries(TEMPLATES).map(([id,t],i)=>`<article class="template-show" style="--accent:${t.accent}"><div class="show-paper">${paper(defaults({template:id,number:`PAIC-00${i+1}`,eventName:t.name,recipient:'الجهة المستهدفة',message:'هذا نموذج مرئي يوضح شخصية القالب وتوزيع عناصر الخطاب قبل استخدامه.',prName:'العلاقات العامة'}))}</div><div class="show-info"><div><span>0${i+1} / ${t.tag}</span><h3>${t.name}</h3></div><p>${t.desc}</p><div class="swatches"><i></i><i></i><i></i></div><a class="primary" href="#/create?template=${id}">استخدام القالب ←</a></div></article>`).join('')}</div></section>`}
function snippets(){const custom=store.snippets();return `<section class="snippets-page"><div class="page-intro"><div><span class="eyebrow">CONTENT LIBRARY</span><h2>النصوص الجاهزة</h2><p>وفر وقتك باستخدام نصوص مرتبة أو عدلها واكتب نصوصك الخاصة.</p></div><button class="primary" data-action="newSnippet">＋ إضافة نص</button></div><div class="snippet-layout"><div class="snippet-list">${[...SNIPPETS.opening,...SNIPPETS.closing,...Object.values(custom).flat()].map((x,i)=>`<article class="snippet-card"><span>0${(i%9)+1}</span><div><b>${esc(x[0])}</b><p>${esc(x[1])}</p></div><button data-insert="${esc(x[1])}">إدراج</button></article>`).join('')}</div><aside class="panel snippet-editor"><span>MY SNIPPET</span><h3>نص مخصص</h3><p>اكتب نصًا يتكرر عندكم واحفظه للاستخدام السريع.</p><label class="field"><span>العنوان</span><input id="snippetTitle" placeholder="مثال: ختام الرعاية"></label><label class="field"><span>النص</span><textarea id="snippetBody" placeholder="اكتب النص هنا..."></textarea></label><button class="primary" data-action="saveSnippet">حفظ النص</button></aside></div></section>`}
function settings(){return `<section class="settings-page"><div class="page-intro"><div><span class="eyebrow">SYSTEM PREFERENCES</span><h2>الإعدادات</h2><p>تحكم في تجربة PAIC Studio من مكان واحد.</p></div></div><div class="settings-grid"><article class="panel setting-card"><span>APPEARANCE</span><h3>المظهر</h3><p>اختر الوضع الداكن أو الفاتح أو دع النظام يقرر تلقائيًا.</p><div class="theme-grid">${[['system','النظام','يتبع جهازك'],['dark','داكن','مظهر الاستديو'],['light','فاتح','مظهر رسمي']].map(([id,n,s])=>`<button class="${state.theme===id?'active':''}" data-theme="${id}"><b>${n}</b><small>${s}</small></button>`).join('')}</div></article><article class="panel setting-card"><span>STORAGE</span><h3>البيانات</h3><p>المحفوظات والمسودات تعمل محليًا، ويمكن مزامنة الخطابات مع السحابة.</p><div class="data-stats"><div><b>${records().length}</b><small>خطابات</small></div><div><b>${store.draft()?'1':'0'}</b><small>مسودة</small></div><div><b>${Object.values(store.snippets()).flat().length}</b><small>نصوص مخصصة</small></div></div><button class="ghost" data-action="sync">مزامنة السحابة</button></article><article class="panel setting-card"><span>WORKSPACE</span><h3>PAIC Studio</h3><p>نسخة ${CONFIG.version} · استديو إنشاء الخطابات الرسمية لنادي البرمجة والذكاء الاصطناعي.</p><div class="about-line"><span>الحالة</span><b class="online">● جاهز للعمل</b></div></article></div></section>`}
function render(){applyTheme();let content=state.page==='home'?home():state.page==='create'?create():state.page==='library'?library():state.page==='templates'?templatesPage():state.page==='snippets'?snippets():settings();shell(content)}
function updateDraft(){store.saveDraft(state.editor);const status=document.querySelector('#saveState');if(status)status.textContent='تم الحفظ تلقائيًا'}
function saveRecord(){const e=state.editor;const errors=validate(e);if(errors.length){notify(`أكمل الحقول: ${errors.join('، ')}`,'error');return}const now=new Date().toISOString();const r=defaults({...e,id:e.id||store.newId(),number:e.number||nextNumber(),createdAt:e.createdAt||now,updatedAt:now});store.saveRecords([r,...records().filter(x=>x.id!==r.id)]);state.editor=r;store.clearDraft();notify('تم حفظ الخطاب');render();syncRecord(r)}
async function syncRecord(r){state.cloud='جاري الحفظ';render();try{await cloudUpsert(r);state.cloud='متصل';notify('تمت مزامنة الخطاب مع السحابة')}catch{state.cloud='محلي';notify('تم الحفظ محليًا — تعذر الاتصال بالسحابة','error')}render()}
async function syncAll(){state.cloud='جاري المزامنة';render();try{await connect();const remote=await cloudList();const local=records();const map=new Map([...remote.map(x=>[x.id,fromCloud(x)]),...local.map(x=>[x.id,x])]);const merged=[...map.values()].sort((a,b)=>new Date(b.updatedAt)-new Date(a.updatedAt));store.saveRecords(merged);for(const r of merged.slice(0,30))await cloudUpsert(r);state.cloud='متصل';notify('تمت المزامنة بنجاح')}catch{state.cloud='محلي';notify('السحابة غير متاحة حاليًا — بياناتك المحلية محفوظة','error')}render()}
function fromCloud(x){return defaults({id:x.id,number:x.letter_number,template:x.template,size:x.size,recipient:x.recipient,recipientName:x.recipient_name,eventName:x.event_name,date:x.date,extraParam:x.extra_param,value:x.value,message:x.message,prName:x.pr_name,prContact:x.pr_contact,prEmail:x.pr_email,digitalStamp:x.digital_stamp,createdAt:x.created_at,updatedAt:x.updated_at})}
function setPageFromHash(){const raw=location.hash.slice(2)||'home';const [page,query]=raw.split('?');state.page=['home','create','library','templates','snippets','settings'].includes(page)?page:'home';if(query){const p=new URLSearchParams(query);const t=p.get('template');if(state.page==='create'&&t&&TEMPLATES[t]){state.editor=defaults({...blank(),template:t,number:nextNumber()});state.step=1;store.saveDraft(state.editor)}}render()}
app.addEventListener('input',e=>{const f=e.target.dataset.field;if(f){state.editor[f]=e.target.type==='checkbox'?e.target.checked:e.target.value;updateDraft();const frame=document.querySelector('#paperFrame');if(frame)frame.innerHTML=paper(state.editor)}});
app.addEventListener('click',e=>{const link=e.target.closest('a[href^="#/"]');if(link){const href=link.getAttribute('href');history.pushState(null,'',href);setPageFromHash();return}const b=e.target.closest('button');if(!b)return;if(b.dataset.page){history.pushState(null,'',`#/${b.dataset.page}`);setPageFromHash();return}if(b.dataset.template){state.editor.template=b.dataset.template;updateDraft();render();return}if(b.dataset.size){state.editor.size=b.dataset.size;updateDraft();render();return}if(b.dataset.step){state.step=Number(b.dataset.step);render();return}if(b.dataset.open){const r=records().find(x=>x.id===b.dataset.open);if(r){state.editor=defaults(r);state.step=1;history.pushState(null,'','#/create');render()}return}if(b.dataset.delete){const id=b.dataset.delete;const r=records().find(x=>x.id===id);store.saveRecords(records().filter(x=>x.id!==id));if(r)cloudDelete(id).catch(()=>{});notify('تم حذف الخطاب');render();return}if(b.dataset.insert){const text=b.dataset.insert;state.editor.message=state.editor.message?`${state.editor.message}\n\n${text}`:text;updateDraft();history.pushState(null,'','#/create');state.page='create';state.step=3;render();return}if(b.dataset.snippet!==undefined){const text=SNIPPETS.opening[Number(b.dataset.snippet)]?.[1]||'';state.editor.message=state.editor.message?`${state.editor.message}\n\n${text}`:text;updateDraft();render();return}const ac=b.dataset.action;if(ac==='theme'){state.theme=state.theme==='dark'?'light':state.theme==='light'?'system':'dark';store.setTheme(state.theme);render();return}if(ac==='menu'){document.querySelector('.sidebar')?.classList.toggle('open');return}if(ac==='reset'){state.editor=defaults({...blank(),number:nextNumber()});state.step=1;store.clearDraft();render();return}if(ac==='next'){if(state.step<3){if(state.step===2&&validate({...state.editor,message:'x'}).some(x=>x==='الجهة'||x==='الفعالية'||x==='التاريخ')){notify('أكمل بيانات الخطاب أولًا','error');return}state.step++;render()}else saveRecord();return}if(ac==='prev'){state.step=Math.max(1,state.step-1);render();return}if(ac==='save'){saveRecord();return}if(ac==='copy'){navigator.clipboard?.writeText(plainText(state.editor));notify('تم نسخ نص الخطاب');return}if(ac==='print'){window.print();return}if(ac==='fit'){state.zoom=CONFIG.zoom.default;render();return}if(ac==='zoomIn'||ac==='zoomOut'){state.zoom=Math.max(CONFIG.zoom.min,Math.min(CONFIG.zoom.max,state.zoom+(ac==='zoomIn'?.05:-.05)));const frame=document.querySelector('#paperFrame');if(frame)frame.style.transform=`scale(${state.zoom})`;const z=document.querySelector('#zoomValue');if(z)z.textContent=Math.round(state.zoom*100)+'%';return}if(ac==='newSnippet'){document.querySelector('#snippetTitle')?.focus();return}if(ac==='saveSnippet'){const title=document.querySelector('#snippetTitle')?.value.trim();const body=document.querySelector('#snippetBody')?.value.trim();if(!title||!body){notify('اكتب عنوان النص ومحتواه','error');return}const c=store.snippets();const id=store.newId();c[id]=[[title,body]];store.saveSnippets(c);notify('تم حفظ النص');render();return}if(ac==='sync'){syncAll();return}if(b.dataset.theme){state.theme=b.dataset.theme;store.setTheme(state.theme);render()}});
window.addEventListener('hashchange',setPageFromHash);matchMedia('(prefers-color-scheme:light)').addEventListener?.('change',()=>{if(state.theme==='system')applyTheme()});setPageFromHash();syncAll();
