import {CONFIG} from './config.js';
import {store} from './store.js';
import {TEMPLATES,SNIPPETS,templateCards} from './templates.js';
import {blank,defaults,validate,plainText,paper} from './document.js';
import {connect,cloudUpsert,cloudList,cloudDelete} from './cloud.js';

const app=document.querySelector('#app');
const toast=document.querySelector('#toast');
const state={page:'home',step:1,query:'',theme:store.theme(),editor:defaults(store.draft()||{}),cloud:'محلي',zoom:window.innerWidth<620?.44:CONFIG.zoom.default,snippetCategory:'all'};
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const notify=(message,type='ok')=>{toast.textContent=message;toast.className=`toast show ${type}`;clearTimeout(notify.timer);notify.timer=setTimeout(()=>toast.className='toast',2800)};
const records=()=>store.records().sort((a,b)=>new Date(b.updatedAt)-new Date(a.updatedAt));
const nextNumber=()=>{const nums=records().map(r=>Number(String(r.number||'').match(/(\d+)$/)?.[1]||0));return `PAIC-${String(Math.max(0,...nums)+1).padStart(4,'0')}`};
const currentTheme=()=>state.theme==='system'?(matchMedia('(prefers-color-scheme:light)').matches?'light':'dark'):state.theme;
function applyTheme(){document.documentElement.dataset.theme=currentTheme()}
function nav(p,icon,text){return `<a class="nav-link ${state.page===p?'active':''}" href="#/${p}"><span class="nav-icon">${icon}</span><span>${text}</span></a>`}
function shell(content){app.innerHTML=`<div class="app-shell"><aside class="sidebar" id="sidebar"><div class="brand"><span class="brand-mark"><img src="assets/logo-club.svg" alt="PAIC"></span><div><b>PAIC</b><small>STUDIO</small></div></div><div class="workspace"><span>WORKSPACE</span><strong>استديو الخطابات</strong></div><nav class="nav">${nav('home','⌂','الرئيسية')}${nav('create','＋','إنشاء خطاب')}${nav('library','▤','المحفوظات')}${nav('templates','▦','القوالب')}</nav><div class="side-tools"><span class="side-label">CONTENT</span><button class="side-button" data-page="snippets">✦ &nbsp; النصوص الجاهزة</button><button class="side-button" data-page="settings">⚙ &nbsp; الإعدادات</button></div><div class="sidebar-footer"><span class="status-dot"></span><div><b>PAIC Cloud</b><small>${esc(state.cloud)}</small></div></div></aside><main class="main"><header class="topbar"><div><span class="eyebrow">PAIC STUDIO / ${label()}</span><h1>${title()}</h1></div><div class="top-actions"><button class="mobile-menu" data-action="menu">☰</button><span class="cloud-pill"><i></i>${esc(state.cloud)}</span><button class="icon-btn" data-action="cycleTheme" title="تغيير المظهر">◐</button><a class="primary" href="#/create">إنشاء خطاب ＋</a></div></header><div class="content">${content}</div></main><nav class="mobile-nav">${[['home','⌂','الرئيسية'],['create','＋','إنشاء'],['library','▤','المحفوظات'],['templates','▦','القوالب']].map(([p,i,t])=>`<a class="${state.page===p?'active':''}" href="#/${p}">${i} ${t}</a>`).join('')}</nav></div>`;applyTheme()}
function label(){return ({home:'OVERVIEW',create:'EDITOR',library:'LIBRARY',templates:'TEMPLATES',snippets:'CONTENT',settings:'SETTINGS'})[state.page]||'PAIC STUDIO'}
function title(){return ({home:'لوحة التحكم',create:'محرر الخطابات',library:'مكتبة الخطابات',templates:'استديو القوالب',snippets:'مكتبة النصوص',settings:'الإعدادات'})[state.page]||'PAIC Studio'}
function home(){const rs=records();const month=rs.filter(r=>r.updatedAt?.slice(0,7)===new Date().toISOString().slice(0,7)).length;const snippetCount=Object.values(SNIPPETS).flat().length+Object.values(store.snippets()).flat().length;return `<section class="hero"><div class="hero-copy"><span class="hero-chip"><i></i> PAIC DOCUMENT STUDIO</span><h2>خطابات رسمية<br><em>بهوية تليق بالنادي.</em></h2><p>أنشئ الخطاب، اختر هويته، استخدم نصًا جاهزًا أو اكتب من عندك، ثم راجع الورقة واحفظها للطباعة والمشاركة.</p><div class="hero-actions"><a class="primary big" href="#/create">ابدأ خطابًا جديدًا ←</a><a class="ghost big" href="#/templates">استكشف القوالب</a></div><div class="hero-trust"><span>✓ حفظ تلقائي</span><span>✓ معاينة مباشرة</span><span>✓ مكتبة نصوص</span><span>✓ جاهز للطباعة</span></div></div><div class="hero-visual"><div class="hero-glow"></div><div class="float-card float-a"><small>LAST LETTER</small><b>${esc(rs[0]?.number||'PAIC-0001')}</b><span>${esc(rs[0]?.eventName||'جاهز لإنشاء أول خطاب')}</span></div><div class="float-card float-b"><small>WORKFLOW</small><b>01 · 02 · 03</b><span>هوية · بيانات · محتوى</span></div><div class="hero-paper-wrap">${paper(defaults({number:rs[0]?.number||'PAIC-0001',eventName:'خطاب رسمي',recipient:'الجهة المستهدفة',date:new Date().toISOString().slice(0,10),message:'مساحة معاينة حية لشكل الخطاب النهائي قبل الحفظ والطباعة.',prName:'العلاقات العامة'}))}</div></div></section><section class="stats"><div class="stat"><small>إجمالي الخطابات</small><strong>${rs.length}</strong><span>محفوظة في مكتبتك</span></div><div class="stat"><small>هذا الشهر</small><strong>${month}</strong><span>خطابات محدثة</span></div><div class="stat"><small>القوالب</small><strong>04</strong><span>هويات مختلفة</span></div><div class="stat"><small>النصوص</small><strong>${snippetCount}</strong><span>جاهزة للاستخدام</span></div></section><section class="home-grid"><div class="panel"><div class="panel-head"><div><span class="eyebrow">RECENT LETTERS</span><h3>آخر الخطابات</h3></div><a href="#/library">عرض الكل ←</a></div>${rs.slice(0,6).map(recordRow).join('')||empty('مكتبتك جاهزة لأول خطاب','ابدأ من القوالب أو افتح المحرر مباشرة.')}</div><div class="panel"><div class="panel-head"><div><span class="eyebrow">QUICK START</span><h3>ابدأ بسرعة</h3></div></div><div class="quick-list">${[['classic','01','خطاب رسمي','كلاسيكي أكاديمي'],['partnership','02','رعاية وشراكة','شراكة وتعاون'],['thanks','03','شكر وتقدير','شكر وتكريم'],['modern','04','دعوة وفعالية','عصري تقني']].map(([id,n,a,b])=>`<a class="quick-item" href="#/create?template=${id}"><span class="quick-no">${n}</span><span><strong>${a}</strong><small>${b}</small></span><i>←</i></a>`).join('')}</div></div></section>`}
function recordRow(r){const t=TEMPLATES[r.template]||TEMPLATES.classic;return `<div class="record-row"><span class="record-icon" style="--accent:${t.accent}">${esc((r.number||'PAIC').slice(-2))}</span><div><strong>${esc(r.eventName||'بدون عنوان')}</strong><small>${esc(r.recipient||'—')} · ${esc(r.number||'—')}</small></div><span class="record-type">${t.name}</span><button data-open="${esc(r.id)}">فتح</button></div>`}
function empty(a,b){return `<div class="empty"><strong>${a}</strong><small>${b}</small><a class="primary" href="#/create">إنشاء خطاب</a></div>`}
function editor(){const e=state.editor;const t=TEMPLATES[e.template]||TEMPLATES.classic;return `<section class="editor-page"><div class="editor-head"><div><span class="eyebrow">DOCUMENT EDITOR</span><h2>${esc(e.number||'خطاب جديد')}</h2></div><div class="editor-actions"><span class="save-state"><i></i><b id="saveState">${e.id?'مسودة محفوظة':'حفظ تلقائي'}</b></span><button class="ghost" data-action="reset">مسودة جديدة</button><button class="primary" data-action="save">حفظ الخطاب</button></div></div><div class="editor-shell"><section class="editor-panel panel"><div class="steps">${step(1,'الهوية','القالب والمقاس')}${step(2,'البيانات','الجهة والتفاصيل')}${step(3,'المحتوى','النص والتواصل')}</div><div class="editor-content">${state.step===1?stepIdentity():state.step===2?stepData():stepContent()}</div><div class="editor-footer"><button class="ghost" data-action="prev" ${state.step===1?'disabled':''}>السابق</button><div><span>${state.step} من 3</span><button class="primary" data-action="next">${state.step===3?'حفظ الخطاب':'متابعة ←'}</button></div></div></section><aside class="preview-panel panel"><div class="preview-head"><div><span>LIVE PREVIEW</span><strong>${esc(t.name)} · ${e.size==='a4'?'A4':'1:1'}</strong></div><div class="preview-tools"><button data-action="fit">ملاءمة</button><button data-action="zoomOut">−</button><b id="zoomValue">${Math.round(state.zoom*100)}%</b><button data-action="zoomIn">＋</button></div></div><div class="preview-stage"><div class="paper-frame" id="paperFrame" style="transform:scale(${state.zoom})">${paper(e)}</div></div><div class="preview-foot"><button class="ghost" data-action="copy">نسخ النص</button><div class="export-actions"><button class="ghost" data-action="downloadPng">PNG</button><button class="primary" data-action="downloadPdf">حفظ PDF</button></div></div></aside></div></section>`}
function step(n,name,sub){return `<button class="step ${state.step===n?'active':''}" data-step="${n}"><b>0${n}</b><span><strong>${name}</strong><small>${sub}</small></span></button>`}
function field(id,label,type='text',wide=false,placeholder=''){const v=state.editor[id]??'';return `<label class="field ${wide?'wide':''}"><span>${label}</span>${type==='textarea'?`<textarea data-field="${id}" placeholder="${placeholder}">${esc(v)}</textarea>`:`<input data-field="${id}" type="${type}" ${type==='checkbox'&&v?'checked':''} value="${esc(v)}" placeholder="${placeholder}" maxlength="1000">`}</label>`}
function stepIdentity(){return `<div class="section-title"><span>01</span><div><h3>اختيار الهوية</h3><p>ابدأ بالشكل الذي يناسب نوع الخطاب ثم حدد المقاس.</p></div></div><div class="template-grid">${templateCards(state.editor.template)}</div><div class="option-row"><div><strong>مقاس الورقة</strong><small>A4 للمخاطبات الرسمية · 1 : 1 للتصاميم المختصرة</small></div><div class="segmented">${['a4','square'].map(x=>`<button class="${state.editor.size===x?'active':''}" data-size="${x}">${x==='a4'?'A4':'1 : 1'}</button>`).join('')}</div></div>`}
function stepData(){return `<div class="section-title"><span>02</span><div><h3>بيانات الخطاب</h3><p>أدخل البيانات التي ستظهر في الورقة النهائية.</p></div></div><div class="form-grid">${field('recipient','الجهة المستهدفة','text',true,'مثال: شركة أو جهة حكومية')}${field('recipientName','عناية / المسؤول','text',true,'الاسم أو المسمى الوظيفي')}${field('eventName','الموضوع / المناسبة','text',true,'مثال: طلب رعاية فعالية')}${field('date','التاريخ','date')}${field('extraParam','نوع الخطاب','text',false,'رعاية / شراكة / دعوة')}${field('value','القيمة أو المبلغ','text',false,'اختياري')}</div>`}
function snippetPicker(){const custom=store.snippets();const customItems=Object.values(custom).flat();const groups={opening:SNIPPETS.opening,body:SNIPPETS.body,closing:SNIPPETS.closing,custom:customItems.map(x=>Array.isArray(x)?x:[x.title,x.body])};const active=state.snippetCategory==='all'?Object.entries(groups):[[state.snippetCategory,groups[state.snippetCategory]||[]]];return `<div class="snippet-box"><div class="snippet-tabs"><button class="${state.snippetCategory==='all'?'active':''}" data-snippet-tab="all">الكل</button>${[['opening','افتتاح'],['body','متن'],['closing','ختام'],['custom','مخصص']].map(([id,n])=>`<button class="${state.snippetCategory===id?'active':''}" data-snippet-tab="${id}">${n}</button>`).join('')}</div><div class="snippet-options">${active.flatMap(([k,items])=>items.map((x,i)=>`<button data-use-snippet="${esc(x[1])}"><span>${esc(x[0])}</span><small>${esc(x[1]).slice(0,100)}${x[1].length>100?'…':''}</small><b>إدراج</b></button>`)).join('')||'<div class="snippet-empty">لا توجد نصوص في هذا القسم.</div>'}</div><button class="ghost full" data-action="manageSnippets">إدارة مكتبة النصوص ←</button></div>`}
function stepContent(){return `<div class="section-title"><span>03</span><div><h3>المحتوى والتوقيع</h3><p>اكتب النص بنفسك أو استخدم مكتبة النصوص الجاهزة ثم عدله بحرية.</p></div></div><div class="content-tools"><div><button class="ghost" data-action="openSnippets">✦ النصوص الجاهزة</button><button class="ghost" data-action="focusMessage">＋ اكتب كلام من عندك</button></div><span>${state.editor.message.trim().length} حرف</span></div>${state.showSnippets?snippetPicker():''}${field('message','نص الخطاب','textarea',true,'اكتب خطابك هنا أو أدرج نصًا جاهزًا ثم عدله كما تريد.') }<div class="form-grid signature-grid">${field('prName','اسم المرسل / العلاقات العامة')}${field('prContact','رقم التواصل')}${field('prEmail','البريد الإلكتروني','email')}${field('digitalStamp','إظهار ختم PAIC','checkbox')}</div>`}
function library(){const rs=records().filter(r=>{const q=state.query.trim().toLowerCase();return !q||[r.number,r.eventName,r.recipient,TEMPLATES[r.template]?.name].join(' ').toLowerCase().includes(q)});return `<section><div class="page-head"><div><span class="eyebrow">DOCUMENT LIBRARY</span><h2>مكتبة الخطابات</h2><p>كل خطاباتك في مكان واحد، مع بحث سريع وفتح وتعديل ونسخ وحذف.</p></div><a class="primary" href="#/create">إنشاء خطاب ＋</a></div><div class="library-toolbar"><div class="search"><input id="librarySearch" value="${esc(state.query)}" placeholder="ابحث بالرقم أو الجهة أو الموضوع..."></div><button class="filter" data-action="clearSearch">مسح البحث</button></div><div class="panel library-list">${rs.length?`<div class="table-head"><span>الخطاب</span><span>الجهة</span><span>القالب</span><span>آخر تعديل</span><span></span></div>${rs.map(r=>{const t=TEMPLATES[r.template]||TEMPLATES.classic;return `<div class="table-row"><div><b>${esc(r.number)}</b><small>${esc(r.eventName||'بدون عنوان')}</small></div><span>${esc(r.recipient||'—')}</span><em style="--accent:${t.accent}">${esc(t.name)}</em><span>${new Date(r.updatedAt).toLocaleDateString('ar-SA')}</span><div class="row-actions"><button data-open="${esc(r.id)}">فتح</button><button data-duplicate="${esc(r.id)}">نسخ</button><button class="danger" data-delete="${esc(r.id)}">حذف</button></div></div>`}).join('')}`:empty('لا توجد نتائج','جرب تغيير كلمة البحث أو أنشئ خطابًا جديدًا.')}</div></section>`}
function templatesPage(){return `<section><div class="page-head"><div><span class="eyebrow">TEMPLATE SYSTEM</span><h2>استديو القوالب</h2><p>كل قالب له شخصية بصرية واضحة وتوزيع مختلف للخطاب.</p></div><a class="primary" href="#/create">فتح المحرر</a></div><div class="template-showcase">${Object.entries(TEMPLATES).map(([id,t],i)=>`<article class="template-show" style="--accent:${t.accent}"><div class="show-paper"><div>${paper(defaults({template:id,number:`PAIC-00${i+1}`,eventName:t.name,recipient:'الجهة المستهدفة',date:'2026-09-20',message:'نموذج مرئي يوضح شخصية القالب وتوزيع عناصر الخطاب قبل استخدامه.',prName:'العلاقات العامة'}))}</div></div><div class="show-info"><div><span>0${i+1} / ${t.tag}</span><h3>${t.name}</h3></div><p>${t.desc}</p><div class="swatches"><i></i><i></i><i></i></div><a class="primary" href="#/create?template=${id}">استخدام القالب ←</a></div></article>`).join('')}</div></section>`}
function snippets(){const custom=Object.values(store.snippets()).flat().map(x=>Array.isArray(x)?x:[x.title,x.body]);const all=[...Object.values(SNIPPETS).flat(),...custom];return `<section><div class="page-head"><div><span class="eyebrow">CONTENT LIBRARY</span><h2>مكتبة النصوص</h2><p>نصوص جاهزة للافتتاح والمتن والختام، مع مساحة لنصوص النادي الخاصة.</p></div><button class="primary" data-action="newSnippet">＋ إضافة نص</button></div><div class="snippet-layout"><div class="snippet-list">${all.map((x,i)=>`<article class="snippet-card"><span>${String(i+1).padStart(2,'0')}</span><div><b>${esc(x[0])}</b><p>${esc(x[1])}</p></div><button data-insert-library="${esc(x[1])}">إدراج في خطاب</button></article>`).join('')}</div><aside class="panel snippet-editor"><span>MY SNIPPET</span><h3>أضف نصًا خاصًا</h3><p>احفظ العبارات التي تستخدمونها كثيرًا لتظهر لك داخل المحرر.</p><label class="field"><span>العنوان</span><input id="snippetTitle" placeholder="مثال: ختام الرعاية"></label><label class="field"><span>النص</span><textarea id="snippetBody" placeholder="اكتب النص هنا..."></textarea></label><button class="primary" data-action="saveSnippet">حفظ النص</button></aside></div></section>`}
function settings(){return `<section><div class="page-head"><div><span class="eyebrow">SYSTEM</span><h2>الإعدادات</h2><p>تحكم في المظهر والبيانات وتجربة الاستديو.</p></div></div><div class="settings-grid"><article class="panel setting-card"><span>APPEARANCE</span><h3>المظهر</h3><p>اختر الوضع الداكن أو الفاتح أو اجعل الاستديو يتبع إعداد جهازك تلقائيًا.</p><div class="theme-grid">${[['system','النظام','يتبع جهازك'],['dark','داكن','الهوية الأساسية'],['light','فاتح','مظهر نظيف ورسمي']].map(([id,n,s])=>`<button class="${state.theme===id?'active':''}" data-theme="${id}"><b>${n}</b><small>${s}</small></button>`).join('')}</div></article><article class="panel setting-card"><span>DATA</span><h3>البيانات والمزامنة</h3><p>الحفظ المحلي يعمل حتى لو لم تتوفر السحابة. عند الاتصال يتم دمج البيانات حسب آخر تعديل.</p><div class="data-stats"><div><b>${records().length}</b><small>خطابات</small></div><div><b>${store.draft()?'1':'0'}</b><small>مسودة</small></div><div><b>${Object.values(store.snippets()).flat().length}</b><small>نصوص خاصة</small></div></div><button class="ghost" data-action="sync">مزامنة الآن</button></article><article class="panel setting-card"><span>WORKSPACE</span><h3>PAIC Studio</h3><p>الإصدار ${CONFIG.version} · استديو الخطابات الرسمي لنادي البرمجة والذكاء الاصطناعي بجامعة الطائف.</p><div class="about-line"><span>حالة التشغيل</span><b>● جاهز للعمل</b></div></article></div></section>`}
function fitPreview(){const stage=document.querySelector('.preview-stage');const frame=document.querySelector('#paperFrame');if(!stage||!frame)return;const available=Math.max(250,stage.clientWidth-18);const fit=Math.min(.78,available/794);state.zoom=Math.max(CONFIG.zoom.min,Math.min(CONFIG.zoom.max,fit));frame.style.transform=`scale(${state.zoom})`;const z=document.querySelector('#zoomValue');if(z)z.textContent=`${Math.round(state.zoom*100)}%`}
function render(){applyTheme();const content=state.page==='home'?home():state.page==='create'?editor():state.page==='library'?library():state.page==='templates'?templatesPage():state.page==='snippets'?snippets():settings();shell(content);if(state.page==='create')requestAnimationFrame(()=>{const frame=document.querySelector('#paperFrame');if(frame)frame.style.transform=`scale(${state.zoom})`;});}
function updateDraft(){store.saveDraft(state.editor);const status=document.querySelector('#saveState');if(status)status.textContent='تم الحفظ تلقائيًا';const frame=document.querySelector('#paperFrame');if(frame)frame.innerHTML=paper(state.editor)}
function saveRecord(){const e=defaults(state.editor);const errors=validate(e);if(errors.length){notify(`أكمل: ${errors.join('، ')}`,'error');return}const now=new Date().toISOString();const r=defaults({...e,id:e.id||store.newId(),number:e.number||nextNumber(),createdAt:e.createdAt||now,updatedAt:now});store.saveRecords([r,...records().filter(x=>x.id!==r.id)]);state.editor=r;store.clearDraft();notify('تم حفظ الخطاب بنجاح');render();syncRecord(r)}
async function inlineExportImages(html){
 const urls=['assets/logo-university.svg','assets/logo-club.svg'];
 for(const src of urls){try{const res=await fetch(src);const blob=await res.blob();const data=await new Promise((ok,no)=>{const fr=new FileReader();fr.onload=()=>ok(fr.result);fr.onerror=no;fr.readAsDataURL(blob)});html=html.split(`src="${src}"`).join(`src="${data}"`)}catch{}}
 return html;
}
async function paperCanvas(){
 const source=document.querySelector('#paperFrame .paper');
 if(!source)throw new Error('paper');
 const isSquare=source.classList.contains('paper-square');
 const w=794,h=isSquare?794:1123;
 const clone=source.cloneNode(true); clone.style.transform='none'; clone.style.margin='0';
 let html=await inlineExportImages(clone.outerHTML);
 const css=await fetch('css/app.css').then(r=>r.text());
 const svg=`<svg xmlns="http://www.w3.org/2000/svg" width="${w}" height="${h}" viewBox="0 0 ${w} ${h}"><foreignObject width="${w}" height="${h}"><div xmlns="http://www.w3.org/1999/xhtml"><style>${css.replace(/<\/style/gi,'')}</style>${html}</div></foreignObject></svg>`;
 const blob=new Blob([svg],{type:'image/svg+xml;charset=utf-8'});const url=URL.createObjectURL(blob);
 try{const img=new Image();img.decoding='async';await new Promise((ok,no)=>{img.onload=ok;img.onerror=no;img.src=url});const canvas=document.createElement('canvas');canvas.width=w*2;canvas.height=h*2;const ctx=canvas.getContext('2d');ctx.fillStyle='#fff';ctx.fillRect(0,0,canvas.width,canvas.height);ctx.drawImage(img,0,0,canvas.width,canvas.height);return canvas}finally{URL.revokeObjectURL(url)}
}
function downloadBlob(blob,name){const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=name;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),1500)}
function safeFileName(name){return String(name||'خطاب-PAIC').replace(/[\\/:*?"<>|]/g,'-').replace(/\s+/g,' ').trim().slice(0,90)||'خطاب-PAIC'}
async function downloadPng(){try{notify('جاري تجهيز PNG…');const c=await paperCanvas();const b=await new Promise(ok=>c.toBlob(ok,'image/png'));downloadBlob(b,`${safeFileName(state.editor.recipient||'جهة')}.png`);notify('تم حفظ ملف PNG')}catch{notify('تعذر إنشاء PNG — جرب مرة أخرى','error')}}
function pdfBytesFromJpeg(jpeg,w,h){const enc=new TextEncoder(),parts=[],offsets=[0],push=s=>parts.push(typeof s==='string'?enc.encode(s):s),len=()=>parts.reduce((n,x)=>n+x.length,0);push('%PDF-1.4\n%âãÏÓ\n');const obj=(n,body)=>{offsets[n]=len();push(`${n} 0 obj\n${body}\nendobj\n`)};obj(1,'<< /Type /Catalog /Pages 2 0 R >>');obj(2,'<< /Type /Pages /Kids [3 0 R] /Count 1 >>');const pageW=595.28; const pageH=h===794&&w===794?595.28:841.89; obj(3,`<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageW} ${pageH}] /Resources << /XObject << /Im1 5 0 R >> >> /Contents 4 0 R >>`); const stream=`q\n${pageW} 0 0 ${pageH} 0 0 cm\n/Im1 Do\nQ\n`;obj(4,`<< /Length ${enc.encode(stream).length} >>\nstream\n${stream}endstream`);offsets[5]=len();push(`5 0 obj\n<< /Type /XObject /Subtype /Image /Width ${w} /Height ${h} /ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${jpeg.length} >>\nstream\n`);push(jpeg);push('\nendstream\nendobj\n');const xref=len();push(`xref\n0 6\n0000000000 65535 f \n`);for(let i=1;i<=5;i++)push(`${String(offsets[i]).padStart(10,'0')} 00000 n \n`);push(`trailer\n<< /Size 6 /Root 1 0 R >>\nstartxref\n${xref}\n%%EOF`);const all=new Uint8Array(len());let at=0;for(const p of parts){all.set(p,at);at+=p.length}return all}
async function downloadPdf(){try{notify('جاري تجهيز PDF…');const c=await paperCanvas();const b=await new Promise(ok=>c.toBlob(ok,'image/jpeg',.94));const buf=new Uint8Array(await b.arrayBuffer());const pdf=pdfBytesFromJpeg(buf,c.width,c.height);downloadBlob(new Blob([pdf],{type:'application/pdf'}),`${safeFileName(state.editor.recipient||'جهة')}.pdf`);notify('تم حفظ ملف PDF')}catch{notify('تعذر إنشاء PDF — جرب مرة أخرى','error')}}
async function syncRecord(r){try{await cloudUpsert(r)}catch{/* local save remains the source of truth */}}
function fromCloud(x){return defaults({id:x.id,number:x.letter_number,template:x.template,size:x.size,recipient:x.recipient,recipientName:x.recipient_name,eventName:x.event_name,date:x.date,extraParam:x.extra_param,value:x.value,message:x.message,prName:x.pr_name,prContact:x.pr_contact,prEmail:x.pr_email,digitalStamp:x.digital_stamp,createdAt:x.created_at,updatedAt:x.updated_at})}
async function syncAll(){state.cloud='جاري المزامنة';try{const remote=(await cloudList()).map(fromCloud);const local=records();const map=new Map();for(const r of remote)map.set(r.id,r);for(const r of local){const old=map.get(r.id);if(!old||new Date(r.updatedAt)>=new Date(old.updatedAt))map.set(r.id,r)}const merged=[...map.values()].sort((a,b)=>new Date(b.updatedAt)-new Date(a.updatedAt));store.saveRecords(merged);for(const r of local)await cloudUpsert(r);state.cloud='متصل';notify('تمت مزامنة البيانات');render()}catch{state.cloud='محلي';notify('تعذر الاتصال بالسحابة — البيانات المحلية سليمة','error');render()}}
function newEditor(template='classic'){state.editor=defaults({...blank(),number:nextNumber(),template});state.step=1;state.showSnippets=false;store.saveDraft(state.editor)}
function setPageFromHash(){const raw=location.hash.slice(2)||'home';const [page,q]=raw.split('?');state.page=['home','create','library','templates','snippets','settings'].includes(page)?page:'home';if(q){const p=new URLSearchParams(q);const t=p.get('template');if(state.page==='create'&&t&&TEMPLATES[t])newEditor(t)}if(state.page!=='create')state.showSnippets=false;render()}
app.addEventListener('input',e=>{if(e.target.id==='librarySearch'){state.query=e.target.value;render();const el=document.querySelector('#librarySearch');el?.focus();el?.setSelectionRange(el.value.length,el.value.length);return}const f=e.target.dataset.field;if(f){state.editor[f]=e.target.type==='checkbox'?e.target.checked:e.target.value;updateDraft()}});
app.addEventListener('click',e=>{const link=e.target.closest('a[href^="#/"]');if(link){const href=link.getAttribute('href');history.pushState(null,'',href);setPageFromHash();return}const b=e.target.closest('button');if(!b)return;if(b.dataset.page){history.pushState(null,'',`#/${b.dataset.page}`);setPageFromHash();return}if(b.dataset.template){state.editor.template=b.dataset.template;updateDraft();render();return}if(b.dataset.size){state.editor.size=b.dataset.size;updateDraft();render();return}if(b.dataset.step){state.step=Number(b.dataset.step);render();return}if(b.dataset.snippetTab){state.snippetCategory=b.dataset.snippetTab;render();return}if(b.dataset.useSnippet){state.editor.message=state.editor.message?`${state.editor.message}\n\n${b.dataset.useSnippet}`:b.dataset.useSnippet;state.showSnippets=false;updateDraft();render();return}if(b.dataset.insertLibrary){newEditor(state.editor.template);state.editor.message=b.dataset.insertLibrary;state.step=3;updateDraft();history.pushState(null,'','#/create');state.page='create';render();return}if(b.dataset.open){const r=records().find(x=>x.id===b.dataset.open);if(r){state.editor=defaults(r);state.step=1;state.showSnippets=false;store.saveDraft(r);history.pushState(null,'','#/create');state.page='create';render()}return}if(b.dataset.duplicate){const r=records().find(x=>x.id===b.dataset.duplicate);if(r){state.editor=defaults({...r,id:'',number:nextNumber(),createdAt:'',updatedAt:''});state.step=1;store.saveDraft(state.editor);history.pushState(null,'','#/create');state.page='create';notify('تم إنشاء نسخة جديدة');render()}return}if(b.dataset.delete){const id=b.dataset.delete;const r=records().find(x=>x.id===id);if(!confirm(`حذف الخطاب ${r?.number||''}؟`))return;store.saveRecords(records().filter(x=>x.id!==id));cloudDelete(id).catch(()=>{});notify('تم حذف الخطاب');render();return}const ac=b.dataset.action;if(ac==='menu'){document.querySelector('#sidebar')?.classList.toggle('open');return}if(ac==='cycleTheme'){state.theme=state.theme==='dark'?'light':state.theme==='light'?'system':'dark';store.setTheme(state.theme);render();return}if(ac==='reset'){newEditor();notify('تم فتح مسودة جديدة');render();return}if(ac==='next'){if(state.step<3){if(state.step===2){const e=validate({...state.editor,message:state.editor.message||'x'});if(e.includes('الجهة')||e.includes('الموضوع')||e.includes('التاريخ')){notify(`أكمل بيانات الخطاب: ${e.filter(x=>['الجهة','الموضوع','التاريخ'].includes(x)).join('، ')}`,'error');return}}state.step++;render()}else saveRecord();return}if(ac==='prev'){state.step=Math.max(1,state.step-1);render();return}if(ac==='save'){saveRecord();return}if(ac==='copy'){navigator.clipboard?.writeText(plainText(state.editor));notify('تم نسخ نص الخطاب');return}if(ac==='focusMessage'){state.step=3;render();setTimeout(()=>document.querySelector('[data-field=message]')?.focus(),0);return}if(ac==='downloadPng'){downloadPng();return}if(ac==='downloadPdf'){downloadPdf();return}if(ac==='print'){window.print();return}if(ac==='fit'){fitPreview();return}if(ac==='zoomIn'||ac==='zoomOut'){state.zoom=Math.max(CONFIG.zoom.min,Math.min(CONFIG.zoom.max,state.zoom+(ac==='zoomIn'?.04:-.04)));const f=document.querySelector('#paperFrame');if(f)f.style.transform=`scale(${state.zoom})`;const z=document.querySelector('#zoomValue');if(z)z.textContent=`${Math.round(state.zoom*100)}%`;return}if(ac==='openSnippets'){state.showSnippets=!state.showSnippets;render();return}if(ac==='manageSnippets'){history.pushState(null,'','#/snippets');state.page='snippets';render();return}if(ac==='newSnippet'){document.querySelector('#snippetTitle')?.focus();return}if(ac==='saveSnippet'){const title=document.querySelector('#snippetTitle')?.value.trim();const body=document.querySelector('#snippetBody')?.value.trim();if(!title||!body){notify('اكتب عنوان النص ومحتواه','error');return}const c=store.snippets();c[store.newId()]=[[title,body]];store.saveSnippets(c);notify('تم حفظ النص');render();return}if(ac==='clearSearch'){state.query='';render();return}if(ac==='sync'){syncAll();return}if(b.dataset.theme){state.theme=b.dataset.theme;store.setTheme(state.theme);render()}});
window.addEventListener('hashchange',setPageFromHash);window.addEventListener('resize',()=>{if(state.page==='create'&&window.innerWidth<620){state.zoom=Math.min(.48,Math.max(.38,(window.innerWidth-28)/794));const f=document.querySelector('#paperFrame');if(f)f.style.transform=`scale(${state.zoom})`;const z=document.querySelector('#zoomValue');if(z)z.textContent=`${Math.round(state.zoom*100)}%`}});matchMedia('(prefers-color-scheme:light)').addEventListener?.('change',()=>{if(state.theme==='system')applyTheme()});
if(!location.hash)history.replaceState(null,'','#/home');setPageFromHash();
